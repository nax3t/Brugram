<h1>List of photos</h1>
<ul>
<% @photos.each do |photo| %>
	<li><%= link_to photo.title, photo_path(photo) %></li>
<% end %>
</ul>
<h3>Other user's public photos</h3>
<ul>
<% @public_photos.each do |photo| %>
	<% if photo.user != current_user %>
	<li><%= link_to photo.title, photo_path(photo) %></li>
	<% end %>
<% end %>
</ul>

<%= render 'back' %>